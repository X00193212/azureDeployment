on:
  workflow_dispatch:
  
  jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Docker
        uses: crazy-max/ghaction-setup-docker@v1
      - run: |
          set -x
          docker version
          docker run hello-world
